import { Box, Typography } from '@mui/material';
import React, { FC } from 'react';

const Stimulation: FC = () => {
    return (
        <Box>
            <Typography>Stimulation</Typography>
        </Box>
    );
}

export default Stimulation;
