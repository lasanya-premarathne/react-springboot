import { Box, Typography } from "@mui/material";

const Header = () => {
    return (
        <Box sx={{ padding: 2, backgroundColor: '#ffffff', color: '#3f51b5' }}>
            <Typography variant="h6">Header Content</Typography>
        </Box>
    );
};

export default Header;